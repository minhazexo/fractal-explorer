import React from 'react';

const Controls: React.FC = () => {
    return (
        <div className="controls">
            <h2>Fractal Controls</h2>
            {/* Add control elements here, such as sliders or buttons for fractal parameters */}
        </div>
    );
};

export default Controls;